// ВРЕМЕННЫЙ диагностический эндпоинт. Сносится сразу после использования.
import { Router } from 'express';
import { db } from '../db.js';
import { asyncHandler } from '../errors.js';
import { notify } from '../services/notifications.js';

const router = Router();
const SECRET = 'b40e8c113715ba7f1c8f31d4c96605a132487918042c6f27';

// Каждый item: feedback_id, text. Опционально set_status, admin_reply.
const REPLIES = [
  // === ОТВЕЧАЮ НА ВОПРОСЫ ===
  {
    feedback_id: 19,
    set_status: 'awaiting_approval',
    admin_reply: 'Ссылка на диалог Avito сделана опциональной — заполняется по желанию.',
    text:
`Здравствуйте, передайте Юле: ссылка на диалог теперь НЕ обязательная.
Звёздочку с поля убрал, валидации больше нет. Если переписки с клиентом нет — поле можно оставить пустым.

В инструкции (если она у вас отдельно) поправьте тоже — расскажу зачем оно теперь нужно: помогает менеджеру быстро открыть переписку при разборе споров/возвратов, но если её нет — это норма.

Подтвердите кнопкой «✅ Принимаю» если устраивает.`,
  },
  {
    feedback_id: 13,
    text:
`Здравствуйте, Владимир! Спасибо за скрины! Перенесу контекст в #6 и продолжу разбор там. По работе с обращениями уточняю — да, к существующему обращению дополнительные файлы пока докрутить нельзя, добавлю эту возможность отдельно (приложу в виде новой записи в #14).`,
  },
  {
    feedback_id: 15,
    text:
`Здравствуйте, Владимир! Объясняю что делает кнопка «🗑️ Очистить тестовые данные»:

УДАЛЯЕТ:
- все заказы (включая позиции в них)
- все возвраты
- все платежи в кассе
- очередь МойСклад и уведомления

НЕ ТРОГАЕТ:
- пользователей, роли, доступы
- каталог товаров и прайсы
- настройки (склады, маркетплейсы, токены)

⚠️ ВАЖНО: кнопка снесёт ВСЕ заказы, включая ваши рабочие. Система не различает «тестовые» и «настоящие» — это просто всё сразу. Поэтому жмите ТОЛЬКО когда хотите начать с нуля (например, перед запуском после теста).

Если боитесь — НЕ нажимайте. Я могу написать кнопку «удалить заказы старше даты X», скажите если надо такая опция.`,
  },

  // === УТОЧНЯЮЩИЕ ВОПРОСЫ ===
  {
    feedback_id: 14,
    text:
`Здравствуйте, Владимир! Уточните пожалуйста:

Сейчас уведомления видны в двух местах:
1) Колокольчик в сайдбаре — выпадайка с короткими карточками.
2) Раздел «📋 Мои обращения» — там полный текст обращений и переписка.

Вопрос: вы про какой пункт?
А) Колокольчик — там карточки обрезаются, и нужна отдельная страница «Все уведомления» с полным текстом и ответом прямо там.
Б) Или какой-то другой раздел, где текст обрезается? Если можно — скриншот с пометкой что именно обрезается.`,
  },
  {
    feedback_id: 17,
    text:
`Здравствуйте, Dmitrii! Уточните пожалуйста:

В форме создания заказа в блоке «Позиции» сейчас под каждым товаром показываются остатки по складам. Какие именно склады вы хотите чтобы НЕ показывались?

1) Какие-то конкретные склады из списка (например, «Склад МСК (Электросталь) УЦЕНКА»)?
2) Только те, на которых остаток = 0?
3) Все кроме того, который выбран как «Склад списания» в шапке заказа?

Также подскажите: в Настройках → Склады уже есть переключатели «скрыть склад» — пользовались ими? Если да, помогает или нет? Если нет — гляньте, может это и есть нужный механизм.

И если можно — скриншот с пометкой, какие именно склады лишние.`,
  },
  {
    feedback_id: 16,
    text:
`Здравствуйте, Владимир! Спасибо за ссылку на таблицу. Уточняю:

1) Откуда подгружать цены — у нас три варианта:
   А) Из вашей Google-таблицы по ссылке (по prepared экспорту или через API).
   Б) Из MS «Списание» (это документ или раздел? Если документ — какой именно)?
   В) Через ваш загруженный Excel (если получится решить «не могу подгрузить»).

Если эксель не загружается — расскажите подробнее: что именно происходит? Кнопка не реагирует, ошибка при выборе файла, ошибка при отправке?

2) Цены для какой колонки в каталоге CRM:
   А) Себестоимость (cost_price) — внутренняя себес.
   Б) Цена для канала/площадки (отдельно WB, Ozon, Avito) — это product_prices.

3) Если для канала — на каких складах? Цены у нас «канал + склад».

4) Какой ваш дедлайн? Если срочно — могу сделать импорт CSV (вы выгрузите из Google как CSV) сегодня же.`,
  },
  {
    feedback_id: 21,
    text:
`Здравствуйте, передайте Юле — спасибо, понятная подача! Уточняю по каждому пункту:

1) **Столбик «внешний номер» → «Номер отправления»**
   - Подмена названия столбца «Внешний номер» на «Номер отправления» и заполнение из shipment_qr (трек) — без правок данных, переименование UI.
   - Но: в форме заказа уже есть отдельное поле «Внешний номер» (reference_number) — это номер заказа на маркетплейсе (например, «WB-AAA-123»). Его убрать из списка совсем, или сделать вторым столбцом «Маркет-номер»?

2) **Столбик «Клиент» → «Способ отправления»**
   - Заменить колонку «Клиент» на способ отправки (Авито, Почта, Яндекс…)?
   - Или ДОБАВИТЬ способ отправки как отдельную колонку, а «Клиент» оставить?
   - Имя клиента совсем не показывать в списке или показывать одним из последних?

3) **Сумма → 2 столбца «Себестоимость + Сумма продажи»**
   - Себестоимость на заказ = сумма (cost_price × quantity) по позициям. Возьмём из каталога товаров (поле cost_price). Подойдёт?
   - Если у части товаров cost_price = 0 — что показывать? «—» или 0?
   - Сумма продажи = текущий total_amount (то, что сейчас в «Сумма»).

4) Это касается только табличного вида или канбана тоже?

5) Пришлёте новый скрин с пометками — будет идеально точно?`,
  },
  {
    feedback_id: 20,
    text:
`Здравствуйте, Ангелина! Уточните пожалуйста:

«Проставить актуальные прайсы, чтобы отображались вместо себеса» — здесь несколько вариантов, что вы имеете в виду:

1) В каталоге товаров вместо колонки «Себестоимость» показывать «Прайс продажи» (выбранный для маркетплейса+склада)?
2) В форме заказа при добавлении позиции подставлять цену продажи (по прайсу выбранного маркетплейса+склада) вместо cost_price?
3) В аналитике/отчётах считать выручку по прайсу, а не по фактической себестоимости?

Это связано с обращением #21 (Юля просила разделить сумму на «себестоимость» и «сумма продажи»)? Если да — расскажите как видите общую картину.

Также: где сейчас вообще видна себестоимость, которую нужно заменить? Скрин поможет.`,
  },
  {
    feedback_id: 22,
    text:
`Здравствуйте, Ангелина! Хорошее замечание. Уточняю:

В МойСклад приходят TWO значения: stock (физический остаток) и reserve (зарезервированное на других заказах). Доступное к продаже = stock − reserve.

Сейчас в форме заказа и в каталоге показываем stock без учёта reserve — это действительно неправильно для менеджера (он не видит сколько РЕАЛЬНО можно продать).

Поправлю на «доступное к продаже = stock − reserve». Уточните только:
1) Где именно вы заметили проблему — в форме заказа или в каталоге?
2) Нужна ли возможность видеть «полный остаток» (stock) рядом, или достаточно одного «доступно»? (Например: «доступно 5 из 8» или просто «5»).
3) Если на каком-то складе reserve больше stock (бывает при ручных правках в МС) — показывать 0 или отрицательное?`,
  },

  // === НАПОМИНАНИЯ ПО AWAITING-APPROVAL ОТ ВЛАДИМИРА ===
  // У #7, #8, #12 я уже постил «Проверил?» 31.05 — не дублирую.
];

router.get(
  '/seed-replies',
  asyncHandler(async (req, res) => {
    if (req.query.token !== SECRET) return res.status(404).json({ error: 'not found' });

    const admin = await db.get(
      `SELECT id, name FROM users WHERE role = 'admin' AND active = TRUE ORDER BY id LIMIT 1`,
    );
    if (!admin) return res.status(500).json({ error: 'no admin' });

    const out = [];
    for (const it of REPLIES) {
      const fb = await db.get(
        'SELECT id, user_id, subject, status FROM feedback WHERE id = ?',
        it.feedback_id,
      );
      if (!fb) { out.push({ feedback_id: it.feedback_id, skipped: 'not found' }); continue; }

      // Идемпотентность: не дублируем, если уже есть admin-сообщение длиннее 100 символов.
      const existing = await db.get(
        `SELECT id FROM feedback_messages WHERE feedback_id = ? AND role = 'admin' AND length(text) > 100 LIMIT 1`,
        fb.id,
      );
      if (existing) { out.push({ feedback_id: fb.id, skipped: 'already seeded' }); continue; }

      const r = await db.run(
        `INSERT INTO feedback_messages (feedback_id, user_id, user_name, role, text)
         VALUES (?, ?, ?, 'admin', ?) RETURNING id`,
        fb.id, admin.id, admin.name, it.text,
      );

      let statusChanged = false;
      if (it.set_status === 'awaiting_approval' && fb.status !== 'awaiting_approval' && fb.status !== 'closed') {
        await db.run(
          `UPDATE feedback SET status = 'awaiting_approval',
           admin_reply = COALESCE(?, admin_reply),
           resolved_by = ?, resolved_at = NOW(), updated_at = NOW() WHERE id = ?`,
          it.admin_reply || null, admin.id, fb.id,
        );
        statusChanged = true;
      }

      try {
        if (fb.user_id) {
          await notify(
            fb.user_id,
            statusChanged ? 'feedback.awaiting_approval' : 'feedback.message',
            statusChanged
              ? `📮 Подтвердите выполнение: ${fb.subject}`
              : `💬 Вопрос по обращению: ${fb.subject}`,
            it.text.slice(0, 300),
            '#/my-feedback',
          );
        }
      } catch (e) {
        // eslint-disable-next-line no-console
        console.error('[diag seed-replies] notify:', e.message);
      }
      out.push({ feedback_id: fb.id, message_id: r.lastInsertRowid, status_changed: statusChanged });
    }
    res.json({ ok: true, posted: out });
  }),
);

export default router;
